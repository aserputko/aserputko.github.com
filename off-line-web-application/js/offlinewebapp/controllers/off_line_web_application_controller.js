(function () {
    offLineWebAppController = Backbone.Controller.extend({
		
        initialize : function (options) {
            this._options = options;
            console.log(this._options);
        }
    });
})();
